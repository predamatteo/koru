/// Padding bottom per le schermate dentro lo shell con `extendBody=true`,
/// così la floating nav bar non copre il contenuto scrollabile.
/// 70 (nav height, con label) + 10 (margin-bottom) + ~36 (gesture nav safe area).
const double kBottomNavClearance = 122;
